# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build disruptive-media`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t disruptive-media`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system disruptive-media && \
          adduser --system -G disruptive-media disruptive-media

COPY dist/apps/disruptive-media disruptive-media/
RUN chown -R disruptive-media:disruptive-media .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix disruptive-media --omit=dev -f install

CMD [ "node", "disruptive-media" ]
